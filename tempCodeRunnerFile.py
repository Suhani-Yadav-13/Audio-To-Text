frame_main_inner = tk.Frame(frame_main, bg="")
# center_frame_content(frame_main, frame_main_inner)

# tk.Label(frame_main_inner, text="Speech-to-Text API", font=('Castellar', 18, 'bold'), bg="SteelBlue1").pack(pady=20)

# entry_file_path = tk.Entry(frame_main_inner, font=('Eras Demi ITC', 12), width=50)
# entry_file_path.pack(pady=10)

# btn_browse = tk.Button(frame_main_inner, text="Browse", command=select_file, font=('castellar', 12, 'bold'), bg="light blue")
# btn_browse.pack(pady=10)

# btn_process = tk.Button(frame_main_inner, text="Process", command=process_file, font=('castellar', 12, 'bold'), bg="green", fg="white")
# btn_process.pack(pady=20)

# progress_bar = ttk.Progressbar(frame_main_inner, mode='indeterminate')
# progress_bar.pack(pady=10)

# # Show login frame initially
# show_frame(frame_login)

# # Start the main loop
# root.mainloop()